sudo: required

notifications:
  email: false

sudo: required

services:
  - docker

language: python

python:
  - 3.6

git:
  depth: false

script:
  - echo "building images"
  - make build
  - make push

deploy:
  - provider: script
    skip_cleanup: true
    script: bash .travis-deploy.sh
    on:
      branch: master
